{
  "schemaVersion": 1,
  "id": "pbd.betterlock",
  "name": "Better Lock",
  "version": "1.0.6",
  "author": "pbaekgaard",
  "description": "Quickshell session lock clone with date/time, power controls, and forgot password prompt.",
  "homepage": "https://github.com/pbaekgaard/pbd.omarchy.betterlock",
  "kinds": [
    "service"
  ],
  "keepLoaded": true,
  "entryPoints": {
    "service": "Service.qml"
  },
  "omarchy": {
    "clonedFrom": "omarchy.lock"
  },
  "defaults": {
    "timeFormat": "hh:mm AP",
    "dateFormat": "dddd, MMMM d",
    "autoSuspend": false,
    "suspendTimer": 300
  },
  "schema": [
    {
      "key": "timeFormat",
      "type": "string",
      "label": "Time format",
      "defaultValue": "hh:mm AP",
      "description": "Format string for lock screen time text"
    },
    {
      "key": "dateFormat",
      "type": "string",
      "label": "Date format",
      "defaultValue": "dddd, MMMM d",
      "description": "Format string for lock screen date text"
    },
    {
      "key": "autoSuspend",
      "type": "boolean",
      "label": "Suspend automatically while locked",
      "defaultValue": false,
      "description": "Suspend the computer after the configured locked duration."
    },
    {
      "key": "suspendTimer",
      "type": "integer",
      "label": "Suspend after (seconds)",
      "min": 0,
      "max": 3600,
      "step": 1,
      "defaultValue": 300,
      "description": "Locked duration before automatic suspend when enabled; 0 suspends immediately."
    }
  ]
}
